{
  "name": "error-404",
  "version": "1.0.0",
  "description": "Error 404 application",
  "main": "server.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "preinstall": "npm install -g node-gyp",
    "build": "bash ./render-build.sh",
    "postinstall": "node -e \"try { require('bcrypt'); console.log('bcrypt installed correctly') } catch(e) { console.log('Bcrypt not installed correctly, attempting to rebuild'); process.exit(0) }\""
  },
  "keywords": [
    "404",
    "error-page"
  ],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^16.3.1",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.11.0",
    "nodemailer": "^6.10.0",
    "razorpay": "^2.9.6"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}